# LingChat v0.5.1

## [0.5.1] - 2026-08-27

## 🐈 更新主题：更轻更快、对话与设置观感、局域网配对同步

### 🎉 更新内容详解：

#### ✨️ Part 1: 启动更轻、运行更顺

- **情绪气泡素材**：从 1920×1080 全屏帧裁成精灵并压缩，体积约 **44MB → 4.58MB**。
- **菜单立绘**：`alona.png` 改为 `alona.webp`（约 147KB）。
- **字体**：菜单字体子集化，约 **4.7MB → 383KB**。
- **前端分包**：Vite `manualChunks`，主 JS 约 **1872KB → 507KB**；生产构建关闭 Vue DevTools。
- **粒子特效**：关闭或无粒子时停止 rAF，桌宠窗口不再挂载光标特效。
- **存档写入**：设置 persist 200ms 防抖；对话框大图单独存储，避免拖慢主存档。

#### 🧀 Part 2: 对话、设置与加载观感

- **情绪气泡**：钉在立绘左上角，带弹出 / 光晕 / 闪光；静止立绘去掉无限呼吸缩放。
- **对话栏**：角色名牌、情绪胶囊、继续键与玻璃拟态对齐。
- **设置页**：卡片玻璃拟态；历史记录空态与桌宠历史一致。
- **加载页**：细进度条 + 百分比。
- **通知 / 成就 / 菜单**：通知按类型显示左边条、无头像不占位；成就绿/金主题对齐；主菜单项支持键盘焦点与按压缩放。

#### 🌈 Part 3: 同步、查询与代码整理

- **局域网同步**：双方填写 6 位配对码后才交换数据，token 不再广播。
- **冒险 / 角色列表**：解锁状态一次批量查询，去掉逐条 N+1。
- **语音播放**：`get_voice_audio` 返回路径，前端 `convertFileSrc`；移除 `get_asset_base64`。
- **情绪识别**：分类改为 `spawn_blocking` 并行等待。
- **死代码清理**：旧 HTTP / WebSocket 客户端、`public/css`、未使用历史 Dialog 组件等。
- **日志窗口**：不再重复挂载字体预取、成就监听、久坐提醒。

### 🔧 Bug 修复与改进

- 热路径去掉多余 `console.log`。
- 对话历史设置页与桌宠历史共用 `useDialogHistory`。
- 角色创建改为直接 `create_character` invoke。

### 💡 特别声明

感谢本项目所有的 contributors，没有你们本项目没有今天的坚持更新，大家辛苦了❤

## 使用注意事项（一定要看！！）

Q：下面的那么多文件，我应该下载哪个呢？

A：`setup.exe` 是 Windows 安装程序，`apk` 是手机。Linux / macOS 选择对应的 `.AppImage` / `.deb` / `.dmg`。

Q：Python 版本呢？

A：Python 版 LingChat 已不再提供发行版，自行从源码部署仍然可以。目前 Python 版本已进入半归档状态。

Q：局域网同步连不上？

A：两边都要填写对方的 6 位配对码，不能只开发现。

Q：更新后立绘或语音是空白？

A：若自定义了 CSP / 资源路径，语音与立绘走 asset protocol；首次安装一般不用管。

---

## 下载文件之前，先看上面的重要信息！！！
